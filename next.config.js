/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['live.staticflickr.com', 'firebasestorage.googleapis.com', 'image.tmdb.org'],
    },
}

module.exports = nextConfig
